rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /presentations/{document} {
      allow read;
      // allow create: if requestMatchesUID();
      // allow update: if resourceMatchesUID() && requestMatchesUID();
      // allow delete: if resourceMatchesUID();
    }
    
    match /awards/{document} {
      allow read;
      // allow create: if requestMatchesUID();
      // allow update: if resourceMatchesUID() && requestMatchesUID();
      // allow delete: if resourceMatchesUID();
    }
    
    match /publications/{document} {
      allow read;
      // allow create: if requestMatchesUID();
      // allow update: if resourceMatchesUID() && requestMatchesUID();
      // allow delete: if resourceMatchesUID();
    }
    
    match /media-commentary/{document} {
      allow read;
      // allow create: if requestMatchesUID();
      // allow update: if resourceMatchesUID() && requestMatchesUID();
      // allow delete: if resourceMatchesUID();
    }
    
    match /mentoring/{document} {
      allow read;
      // allow create: if requestMatchesUID();
      // allow update: if resourceMatchesUID() && requestMatchesUID();
      // allow delete: if resourceMatchesUID();
    }
    
    function requestMatchesUID() {
       // return request.auth.uid == request.resource.data.uid;
       return false;
    }
    
    function resourceMatchesUID() {
      // return request.auth.uid == resource.data.uid;
      return false;
    }
  }
}